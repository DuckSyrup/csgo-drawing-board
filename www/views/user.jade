extends layout

block custHead
	script(type='text/javascript', src='/scripts/user.js')

block content
	div #{user}
	if strats
		div #{user}'s strategies:
		each strat in strats
			div.strat(id='#{strat.name}')
				a(href='/u/#{user}/s/#{strat.name}') #{strat.name}
				|/#{strat.map}